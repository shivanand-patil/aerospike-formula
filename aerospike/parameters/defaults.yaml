# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: aerospike
  rootgroup: root
  config: '/etc/aerospike'
  service:
    name: aerospike
  subcomponent:
    config: '/etc/aerospike-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
